<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thinking.analyselibrarysv" >

    <application android:allowBackup="true"
        android:supportsRtl="true">

        <provider
            android:name="com.thinking.analyselibrarysv.TDContentProviderSV"
            android:authorities="${applicationId}.TDContentProviderSV"
            android:enabled="true"
            android:exported="false" />

        <meta-data
            android:name="com.thinkingdata.sv.analytics.android.MainProcessName"
            android:value="${applicationId}" />

    </application>

</manifest>